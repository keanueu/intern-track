import 'package:flutter/material.dart';

class AdminDirectoryScreen extends StatelessWidget {
  const AdminDirectoryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Admin Directory Screen', style: TextStyle(color: Colors.white)),
    );
  }
}
